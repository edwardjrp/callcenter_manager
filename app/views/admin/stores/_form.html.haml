%fieldset
  %legend
    =" #{controller.action_name.capitalize} #{f.object.class}"
  = f.input :name
  = f.input :address
  = f.input :storeid
  = f.association :city, :include_blank=> false
  = f.input :ip  
  .form-actions
    =f.button :submit
    = link_to 'Cancelar', admin_stores_path, :class => 'btn'