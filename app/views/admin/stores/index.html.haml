- if @stores.any?
  %h3 Tiendas
  =will_paginate @stores
  %table.table.table-striped.table-bordered.table-condensed#stores_list
    %thead
      %th Nombre
      %th Dirección
      %th ip
      %th ciudad
      %th ID
      %th 
    %tbody
      -@stores.each do |store|
        %tr{:data=>{'store-id'=> store.id}, :id=>dom_id(store)}
          %td= best_in_place store, :name, :type => :input, :path=>[:admin, store]
          %td= best_in_place store, :address, :type => :input, :path=>[:admin, store]
          %td= best_in_place store, :ip, :type => :input, :path=>[:admin, store]
          %td= store.city.name
          %td= store.storeid
          %td
            = link_to 'Productos', [:admin,store]
  =will_paginate @stores