$('#dashboard_data').html("<%= escape_javascript(render 'rankings') %>");
_.each($('.data_filter'), function(checkbox){
  var target = $(checkbox);
  if (target.is(':checked')) {
    return $("#" + (target.val())).show();
  } else {
    return $("#" + (target.val())).hide();
  }
});